name: Budget Source Monitor - 사용중지

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: 안내
        run: |
          echo "Budget Source Monitor는 사용하지 않습니다."
          echo "Actions에서 Budget Monitor를 실행하세요."
